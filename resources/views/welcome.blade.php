<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Laravel</title>

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.bunny.net">
    <link href="https://fonts.bunny.net/css?family=figtree:400,600&display=swap" rel="stylesheet" />

    <!-- Styles -->
    <style>
        /* General styles */
        *,
        ::after,
        ::before {
            box-sizing: border-box;
            border-width: 0;
            border-style: solid;
            border-color: #e5e7eb;
        }

        /* Remove default padding and margin from all elements */
        body, h1, h2, h3, h4, h5, h6, p, ul, ol, dl, blockquote, form, fieldset, legend, textarea, input, button, select, table, th, td, figure, figcaption, footer, header, main, aside {
            margin: 0;
            padding: 0;
        }

        /* Normalize font styles */
        body, button, input, select, textarea {
            font-family: Figtree, ui-sans-serif, system-ui, sans-serif, Apple Color Emoji, Segoe UI Emoji, Segoe UI Symbol, Noto Color Emoji;
            font-size: 1rem;
            font-weight: normal;
            line-height: 1.5;
            color: inherit;
        }

        a {
            color: inherit;
            text-decoration: inherit;
        }

        strong, b {
            font-weight: bold;
        }

        hr {
            height: 0;
            color: inherit;
            border-top-width: 1px;
        }

        abbr[title] {
            text-decoration: underline dotted;
        }

        summary {
            display: list-item;
        }

        audio, canvas, embed, iframe, img, object, svg, video {
            display: block;
            max-width: 100%;
            height: auto;
        }

        [hidden] {
            display: none;
        }

        /* Custom styles */
        body {
            --tw-text-opacity: 1;
            color: rgb(31 41 55 / var(--tw-text-opacity));
        }
    </style>
</head>

<body>
    <!-- Your content goes here -->
</body>

</html>
